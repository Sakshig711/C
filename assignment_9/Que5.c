#include<stdio.h>
/*5)Keep scanning a number from user until user enters 0.Print addition of all integers.
(These types of codes are written in your notebook)
*/
int main()
{
	int i,num;
	while(2<3)
	{
		printf("Enter the value = ");
		scanf("%d",&num);
		
		if(num==0)
		{
			printf("%d",num);
			break;
		}
		
	}
	
	
	return 0;
}
